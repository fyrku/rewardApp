import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    position: 'absolute',
    bottom: 30,
    right: 30,
    height: 40,
    width: 40,
    borderRadius: 20,
    backgroundColor: '#000',
  },
});
